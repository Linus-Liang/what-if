class GradingService {

  constructor(){

  }


  calculate(assignments, grades){
      /*
        1. loop over all the graders
        2. find the assignment this grade belongs to
        3. compute score
      */
  }

}


const grader = new GradingService();

const gradedAssignments = grader.calculate(assignments, grades);